{
  "name": "field-service-center-verify",
  "private": true,
  "type": "module",
  "description": "Checks for the Field Service Center. The site itself has no dependencies; only these checks do.",
  "scripts": {
    "verify": "node verify.mjs",
    "quick": "node verify.mjs --quick"
  },
  "devDependencies": {
    "playwright": "^1.49.0"
  }
}
